{%- extends "layout" -%}

{% block title %}Categories - Library{% endblock %}

{% block content %}
{% import "_macros/csrf" as csrf %}
<div class="page-header d-flex justify-content-between align-items-center">
    <div>
        <h1 class="mb-0">Categories</h1>
        <p class="text-muted">Manage subject classifications.</p>
    </div>
    {% if canCreate %}
    <a href="/books/categories/new" class="btn btn-primary text-white">
        <i class="bi bi-plus-lg"></i> New Category
    </a>
    {% endif %}
</div>

{% if categories is empty %}
<div class="alert alert-info bg-dark border-secondary text-light">No categories found.</div>
{% else %}
<div class="card mt-3 bg-dark border-secondary">
    <div class="table-responsive container">
        <table class="table table-hover align-middle mb-0 data-table text-light">
            <thead>
                <tr>
                    <th>Code</th>
                    <th>Name</th>
                    <th>Parent</th>
                    <th class="text-end">Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for cat in categories %}
                <tr>
                    <td>
                        <span class="badge bg-primary">{{ cat.code }}</span>
                    </td>
                    <td>
                        <a href="/books/categories/{{ cat.id }}" class="text-decoration-none text-light">
                            {{ cat.name }}
                        </a>
                    </td>
                    <td class="text-muted">{{ cat.parentId | default("N/A") }}</td>
                    <td class="text-end">
                        {% if canUpdate %}
                        <a href="/books/categories/{{ cat.id }}/edit" class="btn btn-sm btn-outline-secondary" aria-label="Edit {{ cat.name }}">
                            <i class="bi bi-pencil"></i>
                        </a>
                        {% endif %}
                        {% if canDelete %}
                        <form method="post" action="/books/categories/{{ cat.id }}/delete" class="d-inline delete-form"
                              data-confirm-title="Delete Category"
                              data-confirm-message="Delete category <strong>{{ cat.name }}</strong>? This action can be undone.">
                            {{ csrf.token(csrfToken) }}
                            <button type="submit" class="btn btn-sm btn-outline-danger" aria-label="Delete {{ cat.name }}">
                                <i class="bi bi-trash"></i>
                            </button>
                        </form>
                        {% endif %}
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endif %}

{% endblock %}

{% block scripts %}
<script src="/js/iam.js" nonce="{{ securityNonce }}"></script>
<script src="/js/books.js" nonce="{{ securityNonce }}"></script>
{% endblock %}
