{%- extends "layout" -%}

{% block title %}{% if mode == 'edit' %}Edit Language{% else %}New Language{% endif %} - Library{% endblock %}

{% block content %}
<div class="page-header">
    <h1 class="mb-0">
        {% if mode == 'edit' %}Edit Language{% else %}New Language{% endif %}
    </h1>
    <p class="text-muted">Language information.</p>
</div>

<form method="post"
      action="{% if mode == 'edit' %}/books/languages/{{ language.id }}{% else %}/books/languages{% endif %}"
      class="text-light" novalidate>
    {% import "_macros/csrf" as csrf %}
    {{ csrf.token(csrfToken) }}
    <div class="card mt-3 bg-dark border-secondary">
        <div class="card-body bg-dark">
            <div class="row g-3">
                <div class="col-md-6">
                    <label class="form-label" for="code">Code</label>
                    <input type="text" id="code" name="code" class="form-control bg-dark border-secondary text-light {% if code is defined and code %}is-invalid{% else %}{% if language is not null %}is-valid{% endif %}{% endif %}" required
                           value="{% if language is not null %}{{ language.code }}{% endif %}">
                    <div class="invalid-feedback" id="codeError">{% if code is defined and code %}{{ code }}{% endif %}</div>
                </div>
                <div class="col-md-6">
                    <label class="form-label" for="name">Name</label>
                    <input type="text" id="name" name="name" class="form-control bg-dark border-secondary text-light {% if name is defined and name %}is-invalid{% else %}{% if language is not null %}is-valid{% endif %}{% endif %}" required
                           value="{% if language is not null %}{{ language.name }}{% endif %}">
                    <div class="invalid-feedback" id="nameError">{% if name is defined and name %}{{ name }}{% endif %}</div>
                </div>
            </div>
        </div>
    </div>

    <div class="mt-3">
        <button type="submit" id="submitBtn" class="btn btn-primary" disabled>
            <i class="bi bi-check-lg"></i> Save
        </button>
        <a href="/books/languages" class="btn btn-outline-secondary">Cancel</a>
    </div>
</form>

<script nonce="{{ securityNonce }}">
document.addEventListener('DOMContentLoaded', function() {
    var form = document.querySelector('form[novalidate]');
    if (!form) return;
    
    var code = document.getElementById('code');
    var name = document.getElementById('name');
    var codeError = document.getElementById('codeError');
    var nameError = document.getElementById('nameError');
    var submitBtn = document.getElementById('submitBtn');
    var alphaRegex = /^[a-zA-ZÀ-ÿ\s'-]+$/;
    
    function isCodeValid() {
        if (!code.value.trim()) return false;
        if (!/^[a-zA-Z0-9]+$/.test(code.value.trim()) || code.value.trim().length > 20) return false;
        return true;
    }
    
    function isNameValid() {
        if (!name.value.trim()) return false;
        if (!alphaRegex.test(name.value.trim()) || name.value.trim().length < 2) return false;
        return true;
    }
    
    function validateCode() {
        if (!isCodeValid()) {
            if (!code.value.trim()) {
                codeError.textContent = 'Code is required.';
            } else {
                codeError.textContent = 'Code must be alphanumeric and 20 characters or less.';
            }
            code.classList.add('is-invalid');
            code.classList.remove('is-valid');
            return false;
        }
        codeError.textContent = '';
        code.classList.remove('is-invalid');
        code.classList.add('is-valid');
        return true;
    }
    
    function validateName() {
        if (!isNameValid()) {
            if (!name.value.trim()) {
                nameError.textContent = 'Name is required.';
            } else {
                nameError.textContent = 'Name must contain only letters, spaces, hyphens or apostrophes.';
            }
            name.classList.add('is-invalid');
            name.classList.remove('is-valid');
            return false;
        }
        nameError.textContent = '';
        name.classList.remove('is-invalid');
        name.classList.add('is-valid');
        return true;
    }
    
    function updateSubmitButton() {
        var valid = isCodeValid() && isNameValid();
        submitBtn.disabled = !valid;
        return valid;
    }
    
    code.addEventListener('input', function() {
        validateCode();
        updateSubmitButton();
    });
    code.addEventListener('change', function() {
        validateCode();
        updateSubmitButton();
    });
    code.addEventListener('blur', function() {
        validateCode();
        updateSubmitButton();
    });
    
    name.addEventListener('input', function() {
        validateName();
        updateSubmitButton();
    });
    name.addEventListener('change', function() {
        validateName();
        updateSubmitButton();
    });
    name.addEventListener('blur', function() {
        validateName();
        updateSubmitButton();
    });
    
    form.addEventListener('submit', function(e) {
        if (!updateSubmitButton()) {
            e.preventDefault();
        }
    });
});
</script>
{% endblock %}
